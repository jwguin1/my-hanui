# 네이버 캐러셀 미노출 진단

> 2026-08-21. 「일산한의원」 검색 노출 13,558 · 플레이스 1위 · 웹문서 1위인데 CTR 1.1%.
> 우리 블록만 이미지가 없고 아래 경쟁자들은 캐러셀 5~6장을 단다.
> **조사만 한다. 구현하지 않는다.**

## 결론 먼저

원인은 셋이고, 셋 다 **검증기가 볼 수 없는 곳**에 있었다.

| # | 원인 | 상태 |
|---|---|---|
| 1 | **홈(`/`)이 `verify:carousel` 검사 대상에 없다** | TARGETS 6개에 `/` 없음 |
| 2 | **홈 대표 이미지가 1916×479 (비율 4.0)** | 다른 페이지는 1080×1080 (1.0) |
| 3 | **ItemList 이미지가 중복·비정사각** | 7개 중 고유 4종 · 전부 1200×630 (1.9) |

경쟁사는 **500×500 정사각 6장, 중복 0**이다.

---

## 1. `verify:carousel` 이 검사하는 것 — 그리고 안 하는 것

```js
const TARGETS = [ "/pain", "/diet", "/skin", "/doctor", "/about", "/contact" ];
```

**홈(`/`)이 없다.** 6개 타깃에 대해서만 「히어로 비율 1:1 (±2%)」를 검사한다
(`verify-carousel.mjs:222` — `sharp` 로 실제 파일 치수를 읽어 비교).

홈에 대해 검사하는 것은 세 가지뿐이다.

| 검사 | 내용 |
|---|---|
| `/ 홈 title 불변` | 문자열 동결 |
| `/ canonical 불변` | 문자열 동결 |
| `/ og:image 불변` | 문자열 동결 |
| `/ 타깃 6개 전부 링크됨` | 링크 존재 여부 |

전부 **「바뀌지 않았는가」**를 보는 회귀 방어 검사다.
**「캐러셀에 쓸 수 있는 이미지가 있는가」는 아무도 보지 않았다.**

> 73 PASS 는 거짓이 아니다. 다만 **검사 범위 밖에서 문제가 났다.**
> JSON-LD 좌표가 144일 살아남은 것과 정확히 같은 구조다 —
> 정교한 검증기가 있어도 **보지 않는 것은 영원히 통과한다.**

## 2. 홈 대표 이미지 — 비율이 맞지 않는다

`carousel-targets.ts` 주석이 규칙을 명시한다.

> 카드 썸네일은 og:image 가 아니라 **「본문 최상단의 페이지 내 최대 이미지」**에서
> 나온다 → hero 필드가 그 이미지다.

실측:

| 페이지 | 본문 최상단 최대 이미지 | 치수 | 비율 | 포맷 |
|---|---|---|---|---|
| `/pain` | `hero/pain-hero.jpg` | 1080×1080 | **1.00** | jpeg |
| **`/`** | `hero-team.webp` | **1916×479** | **4.00** | webp |

홈에는 `PageHeroBanner` 가 아예 없다 —
`about` · `contact` · `doctor` · `pain` · `diet` · `skin` 에는 있는데 홈에만 없다.

홈의 최대 이미지는 팀 단체사진 배너(4:1 초광폭)다. **카드 썸네일로 쓸 수 없는 비율이다.**

## 3. ItemList 이미지 — 중복 3건 · 비율 1.9

홈의 `ItemList` 는 두 개다.

### `#treatment-list` (진료 안내 7개) — ⚠ 문제

```
1. 급성 통증          /images/pain-og.jpg
2. 만성 통증          /images/pain-og.jpg      ← 중복
3. 교통사고           /images/pain-og.jpg      ← 중복
4. 소화불량           /images/autonomic-og.jpg
5. 이명·어지럼·두통    /images/autonomic-og.jpg  ← 중복
6. 다이어트 처방      /images/diet-og.jpg
7. 잡티 제거          /images/skin-og.jpg

고유 이미지 4종 / 7개
전부 1200×630 (비율 1.90)
```

카드 7장 중 3장이 같은 그림이면 캐러셀로 보이지 않는다.

### `#recent-posts` (의학칼럼 5개) — 이미지는 정상

고유 5종. 다만 전부 `og.png` 1200×630 (비율 1.90) 이라 정사각이 아니다.

---

## 4. 경쟁사 비교 — 「일산한의원.홈페이지.org」

```
title: 일산한의원 | 일산서구·동구 교통사고·다이어트한약·피부·여드름 진료 안내
```

WordPress. JSON-LD 3블록 (`Article` 계열 / `ItemList` / `FAQPage`).

| | 경쟁사 | 우리 |
|---|---|---|
| ItemList 항목 | 6개 | 7개 (진료 안내) |
| **고유 이미지** | **6종 / 6개** | **4종 / 7개** |
| **이미지 치수** | **500×500** | 1200×630 |
| **비율** | **1.00** | **1.90** |
| 포맷 | webp | jpeg |
| 항목 URL | 개별 글 6개 | 진료 페이지 7개 |

**포맷은 원인이 아니다** — 경쟁사도 WebP 를 쓰고 캐러셀이 나온다.
갈리는 것은 **비율(1.00 vs 1.90)과 중복(0 vs 3)** 이다.

경쟁사 항목명도 참고할 만하다 — 전부 `일산{키워드}한의원 …을 알아볼 때 참고해두면 좋은 내용`
형태로 **검색어를 항목명에 직접 넣었다.**

---

## 5. 확인 절차 — 원장

네이버는 캐러셀 채택 여부를 직접 알려주지 않는다. 가능한 확인은 아래 둘이다.

> **네이버 서치어드바이저 → 검증 → 웹페이지 최적화**
> - URL 을 넣으면 제목·설명·대표이미지를 **네이버가 어떻게 읽는지** 보여준다
> - `https://www.ilsanhan.com/` 을 넣고 **대표이미지 항목**을 확인할 것
> - 여기서 이미지가 비어 있거나 팀 배너로 잡히면 §2 가 확인된다
>
> **→ 요약 → 사이트 최적화**
> - 구조화 데이터 인식 항목이 있으면 `ItemList` 가 잡히는지 확인

## 6. 아직 모르는 것

- 네이버가 캐러셀에 요구하는 **정확한 최소 치수**는 공개돼 있지 않다.
  경쟁사 500×500 과 우리 타깃 페이지 1080×1080 이 둘 다 1:1 인 것으로 보아
  **비율이 치수보다 중요해 보이지만 확증은 없다.**
- `ItemList` 만으로 캐러셀이 나오는지, 본문 대표이미지가 함께 필요한지도 확증이 없다.
  경쟁사는 **둘 다** 갖추고 있다 (500×500 이미지 6장이 본문에도 있고 ItemList 에도 있다).

→ 조치를 한 번에 하나씩 넣고 재측정하는 편이 낫다.
   전부 바꾸면 무엇이 효과였는지 알 수 없다.

## 7. 조치 후보 (구현 대기 · 우선순위 순)

| # | 조치 | 근거 | 위험 |
|---|---|---|---|
| 1 | **홈 `#treatment-list` 이미지 중복 제거** — 7개에 각기 다른 이미지 | 중복 3건이 가장 명확한 결함 | 낮음. 이미지 3장 신규 필요 |
| 2 | **홈에 1:1 대표 배너 추가** | 타깃 6개는 전부 있는데 홈만 없음 | 낮음. 기존 `PageHeroBanner` 재사용 |
| 3 | `verify:carousel` TARGETS 에 `/` 추가 | 이번 사고의 근본 원인 | 없음. 검사만 늘린다 |
| 4 | ItemList 이미지를 1:1 파생본으로 | 경쟁사와 유일하게 갈리는 지표 | 중간. OG 이미지 체계와 충돌 검토 필요 |

**3번을 1번보다 먼저 하는 것도 방법이다.** 검사를 먼저 켜면 이후 조치의 효과가
검증기에 그대로 드러난다.
